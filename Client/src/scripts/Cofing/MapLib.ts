class MapLib {
	public constructor() {
	}

	public id: number;
	public name: string;
	public size: number;
	public drop_item: string;
	public drop_num: string;
	public special_chance:number;
	public des: string;

	// public static configs: { [key: number]: MapLib } = {};

	// public static Init(objs: Array<any>): void {
	// 	objs.forEach(v => {
	// 		MapLib.configs[v.id] = v as MapLib;
	// 	})
	// }
}